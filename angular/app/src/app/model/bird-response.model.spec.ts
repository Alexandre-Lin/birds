import { BirdResponse.Model } from './bird-response.model';

describe('BirdResponse.Model', () => {
  it('should create an instance', () => {
    expect(new BirdResponse.Model()).toBeTruthy();
  });
});
